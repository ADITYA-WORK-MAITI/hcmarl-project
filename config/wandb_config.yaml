wandb:
  project: "hcmarl"
  entity: null  # set to your W&B username
  run_naming: "{method}_{n_workers}w_{seed}s"
  tags: ["hcmarl", "safe-marl", "warehouse"]
  metrics:
    - violation_rate
    - cumulative_cost
    - safety_rate
    - tasks_completed
    - cumulative_reward
    - jain_fairness
    - peak_fatigue
    - forced_rest_rate
    - constraint_recovery_time
